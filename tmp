# 50-server.cnf file ####################################################

# Configuration file for MySQL server

	# Configuration section for the MySQL server
[mysqld]
	# Set the user for the MySQL server
user 					= root 
	# Set the port number for the MySQL server
port 					= 3306
	# Set the directory for storing MySQL data
datadir 				= /var/lib/mysql
	# Set the socket file for communication with MySQL server
socket 					= /var/run/mysqld/mysqld.sock
	# Set the IP address to bind MySQL server to all
bind-address 			= 0.0.0.0
	#
pid-file                = /run/mysqld/mysqld.pid



# #!/bin/bash

# # Start the MySQL service
# service mysql start

# # Create database
# mysql -e "CREATE DATABASE IF NOT EXISTS \`$SQL_DATABASE\`;"

# # Create user with password
# mysql -e "CREATE USER IF NOT EXISTS \`$SQL_USER\`@'%' IDENTIFIED BY '$SQL_PASSWORD';"

# # Grant all database privileges to the SQL user
# mysql -e "GRANT ALL PRIVILEGES ON \`$SQL_DATABASE\`.* TO \`$SQL_USER\`@'%' IDENTIFIED BY '$SQL_PASSWORD';"

# # Change the password for the root user in MySQL
# mysql -e "ALTER USER 'root'@'localhost' IDENTIFIED BY '$SQL_ROOT_PASSWORD';"

# # Flush privileges
# mysql -u root -p$SQL_ROOT_PASSWORD -e "FLUSH PRIVILEGES;"

# # Shutdown MySQL
# mysqladmin -u root -p$SQL_ROOT_PASSWORD shutdown

# # Start the MySQL server in safe mode
# exec mysqld_safe

# Start the MySQL service